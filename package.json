{
  "name": "arbitrum-usdc-flashloan-arb-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Arbitrum flash-loan arbitrage bot for USDC-centric direct and triangular opportunities.",
  "main": "bot.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arbitrum",
    "start": "node bot.js",
    "lint": "node --check bot.js && node --check scripts/deploy.js && node --check src/config/arbitrum.js && node --check src/lib/abis.js && node --check src/lib/provider.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5",
    "hardhat": "^2.22.18"
  }
}
